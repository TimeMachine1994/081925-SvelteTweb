1	Main Page
	Users Can Create a New Video Project
	Prompts if the user has an ascii and a video file. 
	Uses google transcoder API to extract the audio into an audio file, then sends that audio to Gladia API, and waits for the JSON response.
    Transcodes the video file into a format that is compatible with quick editing online. What does this look like?
	Sends User to Editing Interface
2	
	