rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper function to check for admin custom claim
    function isAdmin() {
      return request.auth.token.admin == true;
    }

    // Helper function to check for admin emails
    function isAdminEmail() {
      return request.auth != null && 
        (request.auth.token.email == 'austinbryanfilm@gmail.com' ||
         request.auth.token.email.matches('.*@tributestream.com'));
    }

    // Global admin override - admins can access everything
    match /{document=**} {
      allow read, write: if isAdmin() || isAdminEmail();
    }

    match /users/{userId} {
      // Admins can read/write any user document.
      // Regular users can only read/write their own.
      allow read, write: if isAdmin() || (request.auth != null && request.auth.uid == userId);
    }


    match /invitations/{invitationId} {
      // Read: Allowed if you sent the invitation or if you are the recipient.
      allow read: if request.auth != null && (request.auth.uid == resource.data.invitedByUid || request.auth.token.email == resource.data.inviteeEmail);
      // Create: Allowed if you are the sender.
      allow create: if request.auth != null && request.resource.data.invitedByUid == request.auth.uid;
      // Update: Allowed only by the recipient to accept it.
      allow update: if request.auth != null && request.auth.token.email == resource.data.inviteeEmail;
      // Delete: Not allowed for clients.
      allow delete: if false;
    }

    // Funeral Directors collection rules
    match /funeral_directors/{funeralDirectorId} {
      // Only the funeral director can read/write their own document
      allow read, write: if request.auth != null && request.auth.uid == funeralDirectorId;
      
      // Admins can read/write all funeral director documents
      allow read, write: if isAdmin();
    }

    match /memorials/{memorialId} {
      // Admin emails have full access
      function isAdminEmail() {
        return request.auth != null && 
          (request.auth.token.email == 'austinbryanfilm@gmail.com' ||
           request.auth.token.email.matches('.*@tributestream.com'));
      }
      
      // Admins can read/write any memorial.
      // Otherwise, apply existing logic plus funeral director permissions.
      allow read: if isAdmin() || 
                     isAdminEmail() ||
                     resource.data.isPublic == true || 
                     request.auth.uid == resource.data.creatorUid ||
                     request.auth.uid == resource.data.ownerUid ||
                     (request.auth != null && resource.data.funeralDirectorUid == request.auth.uid) ||
                     (request.auth != null && resource.data.funeralDirector.id == request.auth.uid);
      
      allow create: if request.auth != null;
      
      allow update, delete: if isAdmin() || 
                              isAdminEmail() ||
                              (request.auth != null && request.auth.uid == resource.data.creatorUid) ||
                              (request.auth != null && request.auth.uid == resource.data.ownerUid) ||
                              (request.auth != null && resource.data.funeralDirectorUid == request.auth.uid) ||
                              (request.auth != null && resource.data.funeralDirector.id == request.auth.uid && resource.data.permissions.funeralDirectorCanEdit == true);
      
      match /followers/{userId} {
        // A user can only follow/unfollow for themselves.
        allow read, create, delete: if request.auth != null && request.auth.uid == userId;
        allow update: if false; // No updates allowed.
      }

      // Chat messages subcollection
      match /chat/{chatId} {
        // Helper function to get memorial data
        function getMemorial() {
          return get(/databases/$(database)/documents/memorials/$(memorialId)).data;
        }
        
        // Helper function to check if user can access memorial
        function canAccessMemorial() {
          let memorial = getMemorial();
          return memorial.isPublic == true || 
                 request.auth.uid == memorial.creatorUid ||
                 request.auth.uid == memorial.ownerUid ||
                 memorial.funeralDirectorUid == request.auth.uid;
        }
        
        // Helper function to check if user is memorial owner
        function isMemorialOwner() {
          let memorial = getMemorial();
          return request.auth.uid == memorial.ownerUid || 
                 request.auth.uid == memorial.creatorUid ||
                 memorial.funeralDirectorUid == request.auth.uid;
        }
        
        // Read: Anyone can read non-deleted messages if they can access the memorial
        allow read: if request.auth != null && 
                       canAccessMemorial() && 
                       resource.data.isDeleted == false;
        
        // Create: Authenticated users can create messages if memorial is accessible
        allow create: if request.auth != null && 
                         canAccessMemorial() &&
                         request.resource.data.userId == request.auth.uid &&
                         request.resource.data.isDeleted == false;
        
        // Update: Users can edit their own messages (not mark as deleted)
        allow update: if request.auth != null && 
                         resource.data.userId == request.auth.uid &&
                         request.resource.data.userId == request.auth.uid &&
                         request.resource.data.isDeleted == false;
        
        // Delete (soft delete): Users can delete own messages, memorial owners can delete any
        allow update: if request.auth != null && 
                         (resource.data.userId == request.auth.uid || isMemorialOwner() || isAdmin()) &&
                         request.resource.data.isDeleted == true;
      }

    }

    // Blog collection rules - for FireCMS admin access
    match /blog/{blogId} {
      // Allow admin emails to read/write blog posts
      allow read, write: if request.auth != null && 
        (request.auth.token.email == 'austinbryanfilm@gmail.com' ||
         request.auth.token.email.matches('.*@tributestream.com') ||
         isAdmin());
      
      // Allow public read access for published posts (for website display)
      allow read: if resource.data.status == 'published';
    }

    // Schedule edit requests rules
    match /schedule_edit_requests/{requestId} {
      // Users can read their own requests
      allow read: if request.auth != null && request.auth.uid == resource.data.requestedBy;
      
      // Users can create requests (API will validate permissions)
      allow create: if request.auth != null;
      
      // Only admins can update/delete requests
      allow update, delete: if isAdmin() || isAdminEmail();
      
      // Admins can read all requests
      allow read: if isAdmin() || isAdminEmail();
    }

    // Wiki pages - Admin only
    match /wiki_pages/{pageId} {
      allow read, write: if isAdmin() || isAdminEmail();
    }

    // Wiki categories - Admin only
    match /wiki_categories/{categoryId} {
      allow read, write: if isAdmin() || isAdminEmail();
    }

    // Wiki page versions - Admin only (for future use)
    match /wiki_page_versions/{versionId} {
      allow read, write: if isAdmin() || isAdminEmail();
    }

  }
}