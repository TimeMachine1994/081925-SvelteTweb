/**
 * Mock funeral directors for the admin "Funeral Directors" screen.
 * Re-exported from the consolidated `showcase-data.json` (single source of truth).
 * Shape mirrors routes/admin/+page.server.ts `funeralDirectors`.
 */
import data from '../showcase-data.json';

export const funeralDirectors = data.funeralDirectors;
