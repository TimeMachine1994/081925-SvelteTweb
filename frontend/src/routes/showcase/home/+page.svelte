<script lang="ts">
	// Reuse the real home page markup verbatim. It uses only local state (no
	// loader data), so it renders unchanged. CTAs are rewired by the showcase
	// layout's navigation interceptor.
	import RealHome from '../../+page.svelte';
</script>

<RealHome />
