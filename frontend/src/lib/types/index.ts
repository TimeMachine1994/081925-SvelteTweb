export type PackageKey = "solo" | "live" | "legacy";

export interface FormState {
	lovedOneName: string;
	memorialDate: string;
	memorialTime: string;
	locationName: string;
	locationAddress: string;
	website: string;
}